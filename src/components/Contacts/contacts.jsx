import styles from './contacts.module.css';

function Contacts() {
  return (
    <div className={styles.wrapper}>
      <p>
        <b>Contacts</b>
      </p>
    </div>
  );
}

export default Contacts;
